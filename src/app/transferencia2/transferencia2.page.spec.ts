import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Transferencia2Page } from './transferencia2.page';

describe('Transferencia2Page', () => {
  let component: Transferencia2Page;
  let fixture: ComponentFixture<Transferencia2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Transferencia2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
